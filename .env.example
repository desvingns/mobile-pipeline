# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.

# Required for graphify semantic layer (free tier: https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_key_here
