# PHASE NN — <title>

<!-- {{PREFIX}}:plan:gen id=PHASE_NN hash=<8hex> generated=<date> -->
## Goal
<1–3 sentences covering all deliverables of this phase.>

## Anchors (re-read before starting)
- §X.Y <title> — slug:<heading-slug> h:<8hex> (≈L<from>–<to>)

## Prerequisites
- PHASE_MM — done (<what it provides>)

## Deliverables (per module)
- `:module` — <files + brief purpose>

## Task checklist
- [ ] TASK-NN.1 Re-read anchors above. Ask before coding if anything is unclear.
- [ ] TASK-NN.k **<lead noun>** — <verb> <object> (cite decision / §X.Y / ACn / US-x)
- [ ] TASK-NN.k **Visual QA** — (clone only) render <Sxx> and compare against its reference per fidelity/<Sxx>.md; file any divergence
- [ ] TASK-NN.k Update PROGRESS.md.

## Done criteria
- <observable build / test / visual outcomes>

## Verification commands
```bash
cd "$(git rev-parse --show-toplevel)"
# the project's real build/test commands (from CLAUDE.md)
```
<!-- /{{PREFIX}}:plan:gen -->

## Notes for next session
<!-- human-owned — the planner NEVER writes here -->
