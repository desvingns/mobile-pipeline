# {{NAME}} — Codex subagent shim. Generated by install-spec.sh from the canonical spec.
# The logic lives in the markdown spec; this shim just makes Codex run it as a native subagent.
name = "{{NAME}}"
description = "{{DESC}}"
model_reasoning_effort = "{{EFFORT}}"
sandbox_mode = "workspace-write"

developer_instructions = """
Read the canonical agent spec at {{AGENT_DIR}}/agents/{{NAME}}.md and follow it EXACTLY.
Before producing output, read any prompt-library rubric it references at
{{AGENT_DIR}}/skills/app-spec-creator/prompts/<id>.md.
Interpret Claude tool names (Read/Write/Glob/Grep/Bash) as the equivalent Codex file and shell
operations. Return ONLY the structured payload (JSON or the named block) defined in that spec —
no prose around it. Do not spawn descendant agents.
"""
