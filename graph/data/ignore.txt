# Файлы из templates/, которые сознательно не показаны в графе.
# Формат: путь, потом причина отдельной строкой с решёткой.

templates/android/snippets/jbr-detect.sh
#   вспомогательный сниппет поиска JBR, вставляется в скрипты при генерации проекта;
#   самостоятельной ноды в конвейере не образует.

templates/ios/snippets/xcode-detect.sh
#   то же самое для Xcode.
